#ifndef UTILITY_H_
#define UTILITY_H_

#include "../../27/person/person.h" // include the header for the Person class

void read_person_data(Person person_arr[], size_t size);
void write_person_data(Person person_arr[], size_t size);

#endif
